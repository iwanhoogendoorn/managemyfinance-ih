{
	"name": "finance-plugin-ih",
	"version": "0.1.0",
	"description": "A personal finance dashboard, ledger, budgeting, and import pipeline for Obsidian.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"typecheck": "tsc -noEmit -skipLibCheck",
		"test": "vitest run",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "Gaurav Mahadew",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.20.0",
		"obsidian": "^1.5.7",
		"tslib": "2.6.2",
		"typescript": "^5.3.3",
		"vitest": "^4.1.10"
	},
	"dependencies": {
		"read-excel-file": "^9.3.7"
	}
}
